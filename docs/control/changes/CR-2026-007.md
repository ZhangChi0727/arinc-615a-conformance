# CR-2026-007 - Establish the M1 CRS and Applicability Package

| Field | Value |
|---|---|
| Change class | Baseline-affecting requirements/applicability candidate |
| Candidate disposition | ADOPT |
| Authority | `configs/requirements/arinc_615a3_m1_crs.json` |
| Generated view | `docs/control/requirements/ARINC615A3_M1_CRS_REVIEW_VIEW.md` |
| Review gates | RG0 scope and RG1 CRS, same immutable Head |
| External approval | NOT AUTOMATED |

## Decision and boundaries

Use one machine-readable package as the sole authority for M1 source bindings,
scope, coverage, applicability, atomic CRS items, dependencies, gaps, review
metadata, fingerprints and activation. The Markdown view is generated and owns
no state. ARINC 615A-3 receives a complete coverage audit; ARINC 665-5 receives
only a dependency-bounded survey triggered by the active authority and selected
services. Source modality and conformance effect remain separate: source
`SHOULD` is not downgraded to advice, and implemented `MAY` behavior is split
from its conditional constraints.

This change does not modify `src/`, create an EFSM, test purpose, case,
procedure, Project Configuration, execution evidence, baseline or tag, and does
not establish conformance, certification readiness or authority acceptance.
Existing code and public summaries cannot close an external normative source.

## Conditional transition

- Pre-activation: M0 is externally verified; the M1 gate is `NOT YET ESTABLISHED`.
- Draft candidate: M1 disposition is `ADOPT`; RG0 and RG1 are `PENDING-EXTERNAL-INDEPENDENT-REVIEW`.
- Activation event: a named independent reviewer approves the unchanged final
  Head, an ordinary two-parent merge is made, and its second parent equals that
  approved Head.
- Post-activation: the package is approved as qualified M2 input, subject to
  every recorded dependency and gap, without establishing conformance.

Repository automation validates structure and repository-side merge evidence;
it does not manufacture independent approval. M2 remains prohibited until the
joint event is externally verified.

## REWORK disposition after first independent review

The first RG0/RG1 review rejected the earlier page-sentence inventory. The
replacement package uses unique leaf source units for prose sentences, table
rows and sequence-chart events. Every CRS item repeats and must exactly match
its coverage source-unit identity, locator, hash, source modality, conformance
effect, applicability and rationale. Its bilingual review semantics expose the
actor, condition, action, object and observable effect rather than a category
template.

Timing provenance is now typed as a fixed source constant, message-carried
parameter, configuration parameter, symbolic source parameter or
non-normative example. A field representation range is not a deadline. The
two fixed constants retained by this candidate are bound only to their actual
Attachment 4 source units; the DLP equation/example tension remains visible for
RG1 adjudication. RFC 768, 791, 1123, 1350, 1785, 2347, 2348 and 2349, ARINC
664 Parts 2 and 3, ARINC 665-5 and ARINC 645 have separate dependency
identities.

`m1_rg0_source_inventory_anchor.json` detects package-only coordinated edits,
including deletion, locator/hash replacement, timing injection and dependency
identity folding. This is not a claim that automation can read or prove a
proprietary standard: completeness, boundary fidelity and semantic fidelity
remain explicit external RG0/RG1 responsibilities. The renewed review scope is
the complete branch range from the M1 base to the final unchanged Head.

## REWORK disposition after complete renewed review

The renewed review found that the first reconstruction still inferred clauses
from body cross-references, lost Appendix namespaces, retained compound prose
and table co-ownership, used several reversed or generic semantic
dispositions, collapsed timing events, and broadcast one Profile-level trigger
set to every bounded 665-5 item. The corrected candidate therefore adds a
controlled section-span manifest, single-owner atomicity metadata, structured
status-table rows and footnote, source-specific timing event families,
requirement-specific 615A-to-665-5 edges, and data-driven semantic regression
assertions for independently identified high-risk propositions.

The repository records a candidate snapshot, not an approval claim. Formal M1
activation remains the conditional joint event defined above. M2 entry requires
external verification that the exact reviewed Head was jointly approved, that
an ordinary merge has that Head as its second parent, and that main CI passed.
No post-merge status commit is required or authorized by this change record.

CI follows a two-stage policy: pull-request-event CI is the feature-branch
gate, and main-branch push CI is the post-merge gate. A feature-branch push run
is not required and must not be reported as having occurred.

## RG0 PASS / RG1 REWORK disposition at `290c7a43...`

The third independent review passed RG0 and retained RG1 as REWORK. The
correction separates the ARINC 615A and ARINC 665 modality vocabularies,
restores negative/non-failure propositions, splits the checksum-option source
unit into its target acknowledgement and loader validation obligations, and
binds timing records to source-stated events and equations. Requirement-level
615A-to-665-5 edges are now optional: Profile-scope admission is not an edge,
and every emitted edge must identify a shared controlled semantic object and
carry an auditable relation rationale.

Every candidate requirement is represented exactly once in the controlled
semantic-assertion manifest. The manifest binds source identity, complete
semantic tuple including polarity, modality/effect, dependencies, timing
relation and any requirement-level edge. This mechanical binding prevents
post-review drift; it does not replace renewed full-scope RG1 human review.

## WO-M1-2026-002 mechanical closures

The later working-tree candidate applies the WO-M1-2026-002 Must items on top
of Head `290c7a43...` without inheriting that Head's RG0 result. RG0 and RG1
remain `PENDING-EXTERNAL-INDEPENDENT-REVIEW` on any new Head. Draft automation
validates structure, page-account completeness, source-register hash/role
bindings, closed profileScope/rhoRA/activation constants, and assertion
coverage. It still does not verify merge topology or manufacture independent
approval; `mergeEvidence` remains `NOT-YET-PRESENT` until an external reviewer
approves an unchanged Head.

Registered RFC 768/791/1123/1350/1785/2347/2348/2349 and ARINC 664 Parts 2 and
3 identities are now open dependencies in the source register, with DD-018
listed among the controlled technical-direction decisions. Every registered
PDF page is accounted by a section span or an explicit exclusion range.

## Fourth-cycle correction candidate

The fourth review cycle found that page accounting alone did not audit the
three embedded supplement change summaries, table rows did not preserve field
width/repetition/note constraints, timing assertions omitted an independently
owned equation unit, and shared tokens could still manufacture false
requirement-level 615A-to-665 edges. The correction adds a page-by-page
supplement disposition register, structured Table 6.4.1–6.4.9 field
constraints, source-evidence sets and symbolic timing relations, and explicit
665-5 refinement dispositions. No direct requirement edge is emitted merely
because two propositions share an object or word.

Semantic assertions bind the structured semantic tuple and generated bilingual
projections. These controls detect drift after review; they do not establish
that author-generated semantics are source-faithful. Both RG0 and RG1 remain
pending on the new Head, and the complete branch range must be reviewed before
M1 activation.

## WO-M1-2026-003 remediation and theory-debt boundary

All symbolic and message-carried timing propositions now reference at least
one independently owned source unit; the two fixed constants use equal lower
and upper values and may retain their defining sentence. No timing-evidence
item is deferred by this candidate. Direct requirement-level 615A-to-665
refinement remains intentionally deferred to M2 under DD-019; this is a scope
boundary, not an inferred conclusion that no such relations exist.

Under DD-020, the bilingual strings are named generated semantic projections
and are explicitly not independent RG1 evidence. Tautological role, operation,
category and obligation fields were removed. Protocol-file field constraints
now use controlled presence and encoding vocabularies; unresolved
`PROSE-DEFINED` rows, if introduced later, must be registered explicitly.

### Per-Head review outcomes

| Head | Reviewer | RG0 | RG1 | Notes |
|---|---|---|---|---|
| `290c7a43fe8aceba147f32cca558e70562fb351f` | Independent third-cycle reviewer | PASS | REWORK | Superseded by correction |
| `56f5ad137c02a74dc58d0cdd680fce33b5ca9642` | Independent correction reviewer | REWORK | REWORK | Superseded |
| `146ab273c0bf8df7c889bc8d8f08531147937fa2` | Independent fourth-cycle reviewer | REWORK | REWORK | WO-M1-2026-003 issued |
| `557946ba87c3f22aaa1b8f24a8ccfded069abb87` | (to be filled by external reviewer) | PENDING | PENDING | Assistant-side source-fidelity audit produced F-1..F-5 (`docs/review/RR-M1-2026-004-source-fidelity.md`); user selected execution of WO-M1-2026-004; Head is superseded by that correction |
|  |  |  |  | Pending review of the new unchanged Head |

## WO-M1-2026-004 source-fidelity replenishment

The fifth-cycle correction is a source-fidelity check against the local
reference PDFs submitted by the user (ARINC 615A-3, 665-5, 664 Part 2). The
audit report is at `docs/review/RR-M1-2026-004-source-fidelity.md`. The five
findings (F-1..F-5) do not add or remove any CRS requirement but require:

1. Attachment 3 (FIND protocol detail) is reclassified from
   `NON-PROTOCOL-PRODUCT-OR-INFORMATIVE` to `DEFERRED-FIND-M9` across 73
   coverage leaves; Appendix E (Data Loading Over AFDX) is reclassified to
   `DEFERRED-AFDX-DEPLOYMENT-M2-INFRASTRUCTURE-BINDING` across 13 leaves.
   See DD-021.
2. ARINC 664 Part 2 (PDF present with
   `sha256=177594eed4cb…`) is registered as `BOUNDED-PHYSICAL-LINK-REFERENCE`
   in `controlled_sources.json` `sources[]` and in `SAR-2026-001.json`, and
   is bound in CRS `sourceBindings`. `ETHERNET-PHYSICAL-LINK-SEMANTICS` is no
   longer blocked by an `openDependencies` entry, but its capability status
   remains `NOT-ESTABLISHED` pending requirement-level binding review.
3. A new `ARINC-664-7` open dependency is registered, blocking the new
   capability `AFDX-DETERMINISTIC-ETHERNET-SEMANTICS` as the ultimate closure
   condition for Appendix E.
4. IETF RFCs 768, 791, 1123, 1350, 1785, 2347, 2348 and 2349 are kept in
   `openDependencies[]` as public references with a new `publicRetrieval`
   sub-object recording canonical URL, retrieval date, byte count and SHA-256;
   the files themselves are stored under `local-references/rfc/` (covered by
   `.gitignore`) and not committed. See DD-022.
5. `m1_source_section_spans.json` gains a `boundedSourceScopeExclusion` array
   that enumerates the excluded 665-5 clauses (§3 Loadable Media Set Parts,
   §4, §5-onwards) with rationale codes (`BOUNDED-6655-SCOPE-EXCLUSION-*`).
   The existing page-range exclusion's rationale is upgraded from
   `PAGES-BEYOND-615A3-TRIGGERED-BOUNDED-SURVEY` to
   `BOUNDED-6655-SCOPE-EXCLUSION-MEDIA-SET-NOT-USED-BY-ETHERNET-UPLOAD`.

No new requirements are identified as missing under the current bounded
UPLOAD + INFORMATION Profile scope. The following standards remain unacquired
and continue to block the corresponding capabilities; they must be procured
before or during M2:

- ARINC 664 Part 7 (AFDX / deterministic Ethernet) — blocks
  `AFDX-DETERMINISTIC-ETHERNET-SEMANTICS`.
- ARINC 664 Part 3 (Internet-Based Protocols and Services) — blocks
  `AIRCRAFT-INTERNET-NETWORK-SEMANTICS`.
- ARINC 645 (Common Terminology for Data Loading) — blocks four integrity
  capabilities under `GAP-ARINC-645`.

This cycle only lands reclassification, identity registration, public RFC
retrieval metadata, bounded-scope exclusion notes, two design decisions
(DD-021 and DD-022), and three new unit tests. `sync_m1_crs.py --check`,
`sync_project_overview.py --check`, `check_repo_baseline.py`, and `pytest` all
pass; `reviewControl.rg0`, `reviewControl.rg1` and `activation.mergeEvidence`
remain unchanged. Formal M1 activation still requires external independent
review of the new unchanged Head and an ordinary two-parent merge whose
second parent equals that Head.

---

## Sixth-cycle network source reception — WO-M1-2026-005

DD-023 records receipt of ARINC 664P3-1 (2004-12-22) and ARINC 664P7
(2005-06-27). Their title/revision pages and binary identities were checked by
the author. Copies were supplied locally by the user; an independent check of
authorization evidence is not claimed. Source acquisition does not close network
applicability or establish a capability. Both sources remain linked to their open
dependency obligations after being added to the acquisition record and package.

The package now carries `networkReferenceReview`: 17 bounded inspection regions,
17 owner-linked reference relations, and seven explicit issues. These are reference
inspection regions, not new atomic coverage leaves or CRS requirements. The existing
2796 leaves and 384 CRS items are retained, without claiming that these counts prove
network requirement completeness. The generated view exposes both language versions.

The user selected Compliant Network; P3-specific deviations and AFDX are deferred.
RFC 1122 (distinct from RFC 1123) has been retrieved and its public-file identity
registered. This resolves the mode choice and missing-copy issues, not network
conformance. The underlying IPv4/UDP host service is an explicit NOT-ESTABLISHED
infrastructure prerequisite: M2 must plan substantiation before an execution
Configuration may be approved. No complete RFC inventory or stack compliance is
claimed. P3 Profiled tables are not admitted as current protocol requirements.
Independent RG0/RG1 must accept the exact historical editions and this bounded
division of protocol requirements and infrastructure prerequisites. Approval remains pending.

The current 615A loader fragmentation/reassembly obligation is not relaxed by P3
profiling options. P7 example ports and AFDX frame/latency parameters do not
override 615A control ports, blocksize or operation deadlines. The supplied P7
base edition cannot close Appendix E's reference to a future AID supplement.
P4 versus integrator-defined addressing and P7 Attachment 2 / IEEE 802.3 details
remain conditional deployment issues. AFDX is unselected; the four ARINC 645
capabilities and all network capabilities remain NOT-ESTABLISHED.

| Prior action | Disposition | Entry condition / owner |
|---|---|---|
| A-1 direct 615A/665 refinement | DEFERRED-M2 under DD-019 | approved M2 scope and source-specific edge review |
| A-2 RFC-1785/2347 edges | DEFERRED; P3/P7 references inspected | exact active 615A source-unit and option edge review |
| A-3 extra timing equations | DEFERRED-M2; timing attachment is 4 | admitted timing analysis; retain existing independent evidence |
| A-4 requirementKind | DEFERRED | separately approved additive taxonomy work |
| F-1/F-2 FIND/AFDX deferral | retained; no activation | independent scope review; AFDX must be explicitly chosen |
| F-3 bounded 665 exclusion | retained | future service/scope change |
| F-4 P2 source identity | retained; capability unestablished | network applicability and evidence review |
| F-5 label harmonization | Nice / DEFERRED | bounded editorial follow-up |

Structural checks validate complete source identities against the acquisition
record, reconcile reference ownership and targets, and reject deleting pending
obligations or promoting capabilities. Independent assertions protect inspection
records from package-only mutation; they do not prove source fidelity.

Completed author work covers source reception, bounded inspection, trace preparation,
guards and reporting. WO AC-02/04/05 semantic completion remains open; AC-12 awaits
independent review on the final unchanged Head. RG0/RG1 remain pending, reviewHead
remains UNBOUND-DRAFT, and mergeEvidence remains NOT-YET-PRESENT. The final report
and CI evidence belong to the PR description. No post-merge status-only commit is
planned; a later authorized substantive change may record actual approval/merge facts.

# 中文版

# CR-2026-007——建立 M1 CRS 与适用性数据包

| 字段 | 值 |
|---|---|
| 变更分类 | 影响基线的需求／适用性候选 |
| 候选处置 | ADOPT |
| 唯一权威 | `configs/requirements/arinc_615a3_m1_crs.json` |
| 生成视图 | `docs/control/requirements/ARINC615A3_M1_CRS_REVIEW_VIEW.md` |
| 评审门 | 同一不可变 Head 上的 RG0 范围与 RG1 CRS |
| 外部批准 | NOT AUTOMATED |

## 决定与边界

采用一个机器可读数据包作为 M1 来源绑定、范围、覆盖、适用性、原子 CRS、依赖、缺口、评审元数据、指纹和激活的唯一权威；Markdown 视图仅为生成物，不拥有状态。ARINC 615A-3 做完整覆盖审计；ARINC 665-5 仅做由活动权威和所选服务触发的有边界依赖审计。来源原始模态与符合性效果分离：来源 `SHOULD` 不得降为普通建议；`MAY` 能力与“若实现则必须满足”的条件义务拆分。

本变更不修改 `src/`，不创建 EFSM、测试目的、用例、规程、Project Configuration、执行证据、baseline 或 tag，也不建立符合性、认证准备度或权威接受。既有代码与公开摘要不能关闭外部规范来源。

## 条件转换

- 激活前：M0 已外部核验；M1 门为 `NOT YET ESTABLISHED`。
- Draft 候选：M1 处置为 `ADOPT`；RG0/RG1 为 `PENDING-EXTERNAL-INDEPENDENT-REVIEW`。
- 激活事件：具名独立评审者批准不变最终 Head，随后普通两父合并，且第二父等于该 Head。
- 激活后：数据包在全部登记依赖和缺口约束下成为获批的 M2 输入，但不建立符合性。

自动化只校验结构和仓库侧合并证据，不制造独立批准。联合事件经外部核验前禁止 M2。

## 首轮独立评审后的 REWORK 处置

首轮 RG0/RG1 评审否决了此前的页面—句子清单。替代数据包为正文句子、表格行和序列图事件建立唯一叶级来源单元。每个 CRS 项重复其覆盖来源单元的身份、定位符、散列、来源模态、符合性效果、适用性与理由，并由校验器强制逐项一致。双语评审语义明确列出参与者、条件、行为、对象与可观察效果，不再使用类别模板。

时序溯源现区分固定来源常数、消息携带参数、配置参数、符号来源参数及非规范示例；字段表示范围不得充当截止期。本候选只在 Attachment 4 的真实来源单元上保留两个固定常数，并把 DLP 公式／示例之间的张力保留给 RG1 裁决。RFC 768、791、1123、1350、1785、2347、2348、2349，ARINC 664 第 2、3 部分，ARINC 665-5 与 ARINC 645 均拥有独立依赖身份。

`m1_rg0_source_inventory_anchor.json` 用于发现仅修改数据包的协同篡改，包括删除、定位符／散列替换、任意时序注入及依赖身份折叠。这不声称自动化能够读取或证明专有标准；完整性、边界忠实度与语义忠实度仍明确属于外部 RG0/RG1。新一轮评审范围是从 M1 基点到不再变化的最终 Head 的完整分支范围。

## 完整新一轮评审后的 REWORK 处置

完整复审发现，首次重建仍会由正文交叉引用推断条款、丢失 Appendix 命名空间、保留复合正文与表体双重所有权，并存在若干方向反转或通用化的语义处置；时序事件被折叠，Profile 级触发集合也被广播到全部有界 665-5 条目。修正候选因此新增受控 section-span 清单、单一所有者原子性元数据、结构化状态表行与脚注、来源特定的时序事件族、需求级 615A→665-5 边，以及针对独立评审识别的高风险命题的数据驱动语义回归断言。

仓库只记录候选快照，不声明批准。M1 正式激活仍是上文定义的条件联合事件。M2 入口要求外部核验精确受审 Head 已获联合批准、普通合并的第二父正是该 Head，且 main CI 成功。本变更记录不要求也不授权合并后状态提交。

CI 采用两阶段政策：pull-request 事件 CI 是特性分支门禁，main 分支 push CI 是合并后门禁。不要求特性分支 push run，也不得声称其已经发生。

## `290c7a43...` 上的 RG0 PASS／RG1 REWORK 处置

第三次独立评审通过 RG0，但 RG1 仍为 REWORK。本次修正分离 ARINC 615A 与 ARINC 665 的模态词汇，恢复否定／不失败命题，把 checksum option 来源单元拆分为目标硬件确认义务和加载器验证义务，并将时序记录绑定到来源明确规定的事件与方程。需求级 615A→665-5 边现为可选：Profile 范围准入不构成需求边；每条实际生成的边必须指出共享的受控语义对象，并携带可审计的关系理由。

受控语义断言清单恰好一次覆盖每条候选需求，并绑定来源身份、包含极性的完整语义元组、模态／效果、依赖、时序关系及需求级边。该机械绑定用于防止评审后漂移，不替代重新执行的完整范围 RG1 人工评审。

## WO-M1-2026-002 机械关闭

后续工作树候选在 Head `290c7a43...` 之上执行 WO-M1-2026-002 的 Must 项，不继承该 Head 的 RG0 结论。任何新 Head 上的 RG0/RG1 仍为 `PENDING-EXTERNAL-INDEPENDENT-REVIEW`。Draft 自动化校验结构、页账完整性、来源登记散列／角色绑定、封闭的 profileScope/rhoRA/activation 常量以及断言覆盖；它仍不核验合并拓扑，也不制造独立批准。`mergeEvidence` 在外部评审者批准不变 Head 之前保持 `NOT-YET-PRESENT`。

RFC 768／791／1123／1350／1785／2347／2348／2349 与 ARINC 664 第 2、3 部分现已作为开放依赖登记，DD-018 列入受控技术方向决策。登记 PDF 的每一页均由 section span 或明确排除区间入账。

## 第四轮修正候选

第四轮评审发现：仅完成页账并不等于审计嵌入 PDF 的三份 supplement 变更摘要；表格行没有保留字段位宽、重复范围与注释约束；时序断言缺少独立拥有的方程单元；共享词或对象仍可能制造错误的 615A→665 需求级边。本修正新增逐页 supplement 处置登记、Table 6.4.1–6.4.9 结构化字段约束、时序来源证据集合与符号关系，以及显式 665-5 精化处置。不得仅因两个命题共享对象或词汇而生成直接需求边。

语义断言绑定结构化语义元组与生成式双语投影。这些控制用于发现评审后的漂移，不证明作者生成的语义忠实于来源。新 Head 上的 RG0 与 RG1 均保持待评审，M1 激活前必须复审完整分支范围。

## WO-M1-2026-003 修正与理论债务边界

全部符号时序和消息携带时序命题现至少引用一个独立拥有的来源单元；两个固定常量采用相等的上下界，可以保留其定义句作为证据。本候选没有延期任何时序证据项。根据 DD-019，直接需求级 615A→665 精化有意延期到 M2；这是范围边界，并不推断此类关系不存在。

根据 DD-020，双语字符串命名为生成语义投影，并明确不构成独立 RG1 证据。已删除同义的角色、操作、类别和义务字段。协议文件字段约束现使用受控的出现条件与编码词汇；以后若引入 `PROSE-DEFINED` 行，必须显式登记为未决项。

### 各 Head 评审结果

| Head | 评审者 | RG0 | RG1 | 说明 |
|---|---|---|---|---|
| `290c7a43fe8aceba147f32cca558e70562fb351f` | 第三轮独立评审者 | PASS | REWORK | 已被修正取代 |
| `56f5ad137c02a74dc58d0cdd680fce33b5ca9642` | 独立修正评审者 | REWORK | REWORK | 已被取代 |
| `146ab273c0bf8df7c889bc8d8f08531147937fa2` | 第四轮独立评审者 | REWORK | REWORK | 已签发 WO-M1-2026-003 |
| `557946ba87c3f22aaa1b8f24a8ccfded069abb87` | （待外部独立评审者填写） | PENDING | PENDING | 助手侧源文件保真审计发现 F-1..F-5（`docs/review/RR-M1-2026-004-source-fidelity.md`），依用户选择执行 WO-M1-2026-004；Head 已被本轮修正取代 |
|  |  |  |  | 等待评审新的不变 Head |

## WO-M1-2026-004 源文件保真回填

第五轮修正基于用户提交的本地 ARINC 615A-3／665-5／664 Part 2 PDF，进行受控源文件保真核对。审计报告位于 `docs/review/RR-M1-2026-004-source-fidelity.md`。识别的差距（F-1..F-5）没有增加或删除任何 CRS 需求，但要求：

1. 附件 3（FIND 协议详解）从"信息性"重分类为 `DEFERRED-FIND-M9`，覆盖 73 个 leaf；附录 E（AFDX 数据加载）从"信息性"重分类为 `DEFERRED-AFDX-DEPLOYMENT-M2-INFRASTRUCTURE-BINDING`，覆盖 13 个 leaf。详见 DD-021。
2. 将 ARINC 664 第 2 部分（PDF 已提供，`sha256=177594eed4cb…`）作为 `BOUNDED-PHYSICAL-LINK-REFERENCE` 加入 `controlled_sources.json` `sources[]` 与 `SAR-2026-001.json`；同时加入 CRS `sourceBindings`。`ETHERNET-PHYSICAL-LINK-SEMANTICS` 不再由 `openDependencies` 阻塞，但能力状态仍为 `NOT-ESTABLISHED`（等待需求级绑定评审）。
3. 新登记 `ARINC-664-7` 开放依赖，阻塞新增能力 `AFDX-DETERMINISTIC-ETHERNET-SEMANTICS`，作为附录 E 的最终封闭条件。
4. IETF RFC 768／791／1123／1350／1785／2347／2348／2349 以公共源身份留在 `openDependencies[]`，附上 `publicRetrieval` 子对象记录检索 URL、日期、字节数与 SHA-256；文件保存在 `local-references/rfc/`（`.gitignore` 覆盖）而不入库。详见 DD-022。
5. `m1_source_section_spans.json` 新增 `boundedSourceScopeExclusion` 数组，明确列出 665-5 §3（Loadable Media Set Parts）、§4、§5 及后续段落被排除的 clause 与 rationaleCode（`BOUNDED-6655-SCOPE-EXCLUSION-*`）。原页面区间排除条目的 rationaleCode 也从 `PAGES-BEYOND-615A3-TRIGGERED-BOUNDED-SURVEY` 更新为 `BOUNDED-6655-SCOPE-EXCLUSION-MEDIA-SET-NOT-USED-BY-ETHERNET-UPLOAD`。

未识别未提供标准所需的新条目；下列标准的缺失继续阻塞对应能力，需在 M2 期间获取：

- ARINC 664 Part 7（AFDX / 确定性以太网）——阻塞 `AFDX-DETERMINISTIC-ETHERNET-SEMANTICS`
- ARINC 664 Part 3（Internet-Based Protocols and Services）——阻塞 `AIRCRAFT-INTERNET-NETWORK-SEMANTICS`
- ARINC 645（Common Terminology for Data Loading）——阻塞 4 项完整性能力（`GAP-ARINC-645`）

本轮修正只落地重分类、身份登记、公共 RFC 检索记录、边界排除注释与两条设计决策，并新增 3 项单元测试。`sync_m1_crs.py --check`、`sync_project_overview.py --check`、`check_repo_baseline.py`、`pytest` 全部通过；`reviewControl.rg0`、`reviewControl.rg1` 与 `activation.mergeEvidence` 保持不变。M1 正式激活仍需外部独立评审新的不变 Head 并完成普通两父合并。

## 第六轮网络来源接收——WO-M1-2026-005

DD-023 记录 ARINC 664P3-1（2004-12-22）与 ARINC 664P7（2005-06-27）的接收。
作者核对了标题／版本页与二进制身份。文件由用户本地提供，未声称独立确认其授权依据。
取得来源不关闭网络适用性，也不建立能力。两份来源加入接收记录和 package 后仍关联其开放依赖义务。

package 新增 `networkReferenceReview`：17 个有边界检查区域、17 条关联所有者的引用关系和七项显式问题。
这些是引用检查区域，不是新增原子覆盖叶或 CRS 需求。保留既有 2796 个叶与 384 条 CRS，
但不声称数量证明网络需求完整。生成视图展示英中两种版本。

用户已选择 Compliant Network；P3 特有偏差与 AFDX 延期。
RFC 1122（不同于 RFC 1123）已取得并登记公共文件身份。
这解决网络模式选择与缺少副本问题，不代表网络符合性成立。
底层 IPv4/UDP 主机服务作为显式 NOT-ESTABLISHED 基础设施前提保留：
M2 须规划其验证，之后才可批准执行 Configuration。未声称完整 RFC 清单或协议栈符合性。
P3 Profiled 表格不纳入当前协议需求。独立 RG0/RG1 须接受精确历史版次，
并复核协议需求与基础设施前提之间的有边界划分。批准仍待完成。

P3 裁剪选项不放宽现有 615A 加载器分片／重组义务。P7 示例端口与 AFDX 帧／延迟参数
不覆盖 615A 控制端口、块大小或操作时限。所提供 P7 初版不能关闭附录 E 的未来 AID 补充版引用。
P4 或集成商自定寻址，以及 P7 附件 2／IEEE 802.3 细节仍是条件部署问题。
AFDX 未选择；ARINC 645 四项能力及所有网络能力保持 NOT-ESTABLISHED。

| 既有行动项 | 处置 | 进入条件／负责阶段 |
|---|---|---|
| A-1 615A/665 直接精化 | 按 DD-019 DEFERRED-M2 | 已批准 M2 范围及逐来源边评审 |
| A-2 RFC-1785/2347 边 | 延期；已检查 P3/P7 引用 | 精确活动 615A 来源单元及选项边评审 |
| A-3 额外时序方程 | DEFERRED-M2；时序位于附件 4 | 纳入时序分析；保留既有独立证据 |
| A-4 requirementKind | 延期 | 另行批准的增量分类工作 |
| F-1/F-2 FIND/AFDX 延期 | 保留，未激活 | 独立范围评审；AFDX 须显式选择 |
| F-3 有边界 665 排除 | 保留 | 后续服务／范围变更 |
| F-4 P2 来源身份 | 保留；能力未建立 | 网络适用性与证据评审 |
| F-5 标签统一 | Nice／延期 | 有边界编辑性跟进 |

结构检查将完整来源身份与接收记录对账，核对引用所有者和目标，并拒绝删除待审义务或晋级能力。
独立断言防止仅修改 package 篡改检查记录；它们不证明来源忠实度。

已完成的作者工作包括来源接收、有边界检查、追踪准备、门禁和报告。
WO AC-02/04/05 的语义完成仍开放；AC-12 等待最终不变 Head 上的独立评审。
RG0/RG1 保持 pending，reviewHead 保持 UNBOUND-DRAFT，mergeEvidence 保持 NOT-YET-PRESENT。
最终报告和 CI 证据写入 PR 描述。不计划专门 post-merge 状态提交；后续获授权的实质变更可记录真实批准／合并事实。
