% Readable derived display of ALG-CLTAV-03; the accepted source remains unchanged.
\begin{algorithm}[H]
\caption{\textsc{InterpretOutcome}: execution and observation coordination (ALG-CLTAV-03, S6)}
\label{alg:cltav-03}
\KwIn{ExecutionResult $er$; SelectSnapshot $\xi$; $\Gamma,\eta$; uncertainty reference $U$}
\KwOut{total Outcome $z$, including effect, validity, $I_z$, and confirmation fields}
$z\leftarrow\textsc{NewOutcome}()$; $z.\mathrm{effect}\leftarrow er.\mathrm{effect}$; $z.\mathrm{kind}\leftarrow\xi.\mathrm{actionKind}$; $z.\mathrm{actionId}\leftarrow\xi.\mathrm{actionId}$\;
\If{$er.\mathrm{effect}\in\{\texttt{CONFIRMED-NOT-SENT},\texttt{UNKNOWN-EFFECT}\}$}{\Return $z$\;}
$(I_z,e,s,p,o,m)\leftarrow\IFobs(\Gamma,er.\mathrm{record},\mathrm{clocks},\varepsilon)$\;
\If{$o$ is invalid}{set $z.\mathrm{validity}\leftarrow\mathrm{ERROR}$ and $z.\mathrm{effect}\leftarrow\texttt{UNKNOWN-EFFECT}$; \Return $z$\;}
$\mathrm{timing}\leftarrow\textsc{InterpretTimedObservation}(o,m,\xi,N_r,D_r,\mathrm{Match}_r)$\;
\If{$\mathrm{timing}.\mathrm{verdict}=\mathrm{ERROR}$}{set $z.\mathrm{validity}\leftarrow\mathrm{ERROR}$ and $z.\mathrm{effect}\leftarrow\texttt{UNKNOWN-EFFECT}$; \Return $z$\;}
set $z.\mathrm{validity}\leftarrow\mathrm{timing}.\mathrm{verdict}$ and $z.I_z\leftarrow I_z$\;
\If{$\xi.\mathrm{actionKind}\in\{\mathrm{PREP},\mathrm{RECOVER}\}$}{
  \tcp{\IFprep{} returns values only; it does not write $\Gamma$ or $\eta$}
  $z.\mathrm{prepResultEvaluated}\leftarrow\textbf{true}$; $(z.\mathrm{targetConfirmed},s,z.\mathrm{prepErr},z.\mathrm{declaredTarget},z.\mathrm{evidence},p)\leftarrow$ \IFprep$(\Gamma,\eta,\xi.\mathrm{actionId},er.\mathrm{record})$
}
set $z.\mathrm{summaryConfirmed}\leftarrow s$ and $z.\mathrm{postSummary}\leftarrow p$\;
\If{$e=\texttt{UNKNOWN-EFFECT}$, $z.\mathrm{prepErr}$, an unconfirmed Prep summary, an unconfirmed Recover target, or $e=\texttt{NONE}$ with no confirmed summary}{set $z.\mathrm{effect}\leftarrow\texttt{UNKNOWN-EFFECT}$\;}
\Else{set $z.\mathrm{effect}\leftarrow e$\;}
\tcp{all $\Gamma$/$\eta$ writes stay in ALG-CLTAV-04 and S9}
\Return $z$\;
\end{algorithm}
