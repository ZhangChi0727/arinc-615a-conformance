# CR-2026-011 — Successor M1 delta for 6.4.4/6.4.5 identities and LUR WRQ actor (T1)

| Field | Value |
|---|---|
| Change class | Successor M1 delta (new blob OIDs; frozen merge preserved) |
| Change objective | Execute CR-2026-009 Items A and B, plus the owner-authorized 6.4.5 LUR→LUS companion required for CRS ordinal uniqueness |
| Source register | `configs/research/controlled_sources.json` |
| Decisions | DD-027 (primary). DD-025 remains the input-acceptance versus action-closure rule. |
| Candidate disposition | ADOPT successor identities; not an M2 final approval |
| Formal activation | Independent incremental RG1 must accept this delta; RG0/RG2 remain external |
| Authorization | CR-2026-009 (Items A, B, 664P3-1). Owner option 1 on 2026-09-10 for the 6.4.5 companion. |
| Related | CR-2026-008, CR-2026-009 |

## Scope executed

1. **Item A — Table 6.4.4-1.** `fieldConstraint.protocolFile` for CRS-M1-00309 through CRS-M1-00315 changes LUI→LUR. Source unit ids remain `SU-ARINC-615A-3-TABLE-6_4_4-1-R00*`. CRS-M1-00143 keeps its prose locator and HEADER-FILE object; LUR is added so the clause page cluster names the protocol file identified by visual check of PDF p.84–85.
2. **Companion — Table 6.4.5-1.** All `fieldConstraint.protocolFile` values currently LUR on clause 6.4.5 change LUR→LUS. This is required: `sync_m1_crs.py` demands unique ordinals per protocol file. Leaving 6.4.5 as LUR after Item A fails that gate. Field morphology (upload status code, timers, ratios) is the LUS table, not a second LUR list. 6.4.6 remains DEFERRED-DOWNLOAD-M9.
3. **Item B — CRS-M1-00365.** `semantic.actor` TARGET-HARDWARE→DATA-LOADER. `receiver` remains DLA. Projections refresh to match.
4. **664P3-1.** `blockingInputs.NET-ISSUE-EDITION.status` becomes `ACCEPTED-CURRENT-EDITION-P3-1-AFDX-DEFERRED`. 664P7 stays recorded; AFDX remains unselected.
5. **Known gap.** After Item A there is no dedicated LUI field table in this CRS. LUI remains a sequence file on the M2 machine. The gap is named; LUI fields are not invented.

## Identity and non-transplant

Historical merge `9bf18124d405b656815bc9eb524ae29bb4f04f56` and its frozen blobs stay a preserved record. Successor `inputAcceptance` binds worktree/HEAD blobs as a new identity and does not transplant the original merge approval. `reviewControl.blocksFinalApproval` stays true until independent RG1 accepts and RG2 passes. This CR does not self-APPROVE RG0/RG1/RG2.

## M2 rebind

Field-constraint ids follow `FC-{protocolFile}-{fieldId}` after the new identities (LUR from 6.4.4, LUS from 6.4.5). `m1FileIdentityConflict` is false when those identities hold. 615A→665 refinements from CRS-M1-00143 and CRS-M1-00315 become candidate active edges. Artifact version is M2-CANDIDATE-5.

## Non-authorizations

No edit outside the successor-delta rows above, except generated views, semantic assertions bound to those rows, M2 rebind, and the validator/schema changes that allow a successor identity without treating OPEN-M1-CORRECTION as silently closed. ARINC 645 is not acquired. AFDX is not selected.

# 中文版

# CR-2026-011——6.4.4／6.4.5 身份与 LUR WRQ 参与者的后继 M1 增量（T1）

| 字段 | 值 |
|---|---|
| 变更分类 | 后继 M1 增量（新 blob；冻结合并保留） |
| 变更目标 | 执行 CR-2026-009 事项 A、B，以及所有者于 2026-09-10 选择的 6.4.5 LUR→LUS 伴随纠正（否则 CRS 序号唯一性门禁失败） |
| 来源登记 | `configs/research/controlled_sources.json` |
| 决策 | DD-027（主决策）。DD-025 仍为输入接受与行动关闭规则。 |
| 候选处置 | ADOPT 后继身份；不是 M2 最终批准 |
| 正式激活 | 须由独立增量 RG1 接受；RG0／RG2 仍为外部 |
| 授权 | CR-2026-009（事项 A、B、664P3-1）。6.4.5 伴随为 2026-09-10 所有者选项 1。 |
| 关联 | CR-2026-008、CR-2026-009 |

## 已执行范围

1. **事项 A——表 6.4.4-1。** CRS-M1-00309 至 CRS-M1-00315 的 `fieldConstraint.protocolFile` 由 LUI 改为 LUR。source unit id 仍为 `SU-ARINC-615A-3-TABLE-6_4_4-1-R00*`。CRS-M1-00143 保留散文定位与 HEADER-FILE 对象，并增加 LUR，使该条款页簇标明 PDF 第 84–85 页目视核对所确认的协议文件。
2. **伴随——表 6.4.5-1。** 条款 6.4.5 上原为 LUR 的字段谓词全部改为 LUS。这是必要伴随：`sync_m1_crs.py` 要求同一 protocolFile 的 ordinal 唯一。事项 A 之后若 6.4.5 仍为 LUR，该门禁失败。字段形态（上传状态码、计时器、比率）是 LUS 表，不是第二张 LUR 列表。6.4.6 保持 DEFERRED-DOWNLOAD-M9。
3. **事项 B——CRS-M1-00365。** `semantic.actor` 由 TARGET-HARDWARE 改为 DATA-LOADER。`receiver` 保持 DLA。投影同步刷新。
4. **664P3-1。** `blockingInputs.NET-ISSUE-EDITION.status` 改为 `ACCEPTED-CURRENT-EDITION-P3-1-AFDX-DEFERRED`。664P7 保持已登记；AFDX 未选。
5. **已知缺口。** 事项 A 之后本 CRS 没有专用 LUI 字段表。LUI 仍是 M2 机器上的序列文件。缺口具名；不编造 LUI 字段。

## 身份与不移植

历史合并 `9bf18124d405b656815bc9eb524ae29bb4f04f56` 及其冻结 blob 作为保留记录不变。后继 `inputAcceptance` 绑定工作区／HEAD blob 作为新身份，不移植原合并批准。`reviewControl.blocksFinalApproval` 在独立 RG1 接受且 RG2 通过前保持 true。本 CR 不自批 RG0／RG1／RG2。

## M2 重绑定

字段约束 id 按纠正后的 `FC-{protocolFile}-{fieldId}`（6.4.4 为 LUR，6.4.5 为 LUS）。身份成立时 `m1FileIdentityConflict` 为 false。CRS-M1-00143 与 CRS-M1-00315 的 615A→665 精化成为候选活动边。制品版本为 M2-CANDIDATE-5。

## 非授权

除上述后继 delta 行、其生成视图与语义断言、M2 重绑定，以及允许后继身份、同时禁止把 OPEN-M1-CORRECTION 静默关闭的校验器／schema 变更外，不编辑其他 `configs/requirements/`。不获取 ARINC 645。不选择 AFDX。
