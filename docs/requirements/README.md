# Requirements drafts (shared)

Place the evolving **verification-point catalog** here (e.g. `verification_points.md`).

- Track A turns these into executable scenarios under `tests/scenarios/` / `configs/`.  
- Track B formalizes them as the thesis §3 “conformance requirements model.”

Do not paste internal ICD text verbatim if this repo may be shared publicly.
