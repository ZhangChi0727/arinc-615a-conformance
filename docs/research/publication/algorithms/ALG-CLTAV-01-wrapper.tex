% Wrapper for the CL-TAV algorithm specification package.
% Main text: ALG-CLTAV-01 (top level) and the three core procedures
% ALG-CLTAV-05 (selection), ALG-CLTAV-06 (timed observation), ALG-CLTAV-07 (update).
% Appendix: the S2/S6/S7--S8 coordination wrappers ALG-CLTAV-02/03/04 and the
% contract tables. Not a second method; selects no solver technology.
\documentclass[11pt]{article}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{Latin Modern Roman}
\setCJKmainfont{SimSun}
\usepackage{amsmath,amssymb}
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\usepackage{float}
\usepackage[margin=0.75in]{geometry}
\SetAlgoNlRelativeSize{0}
\SetAlFnt{\scriptsize}
\SetAlgoSkip{smallskip}
\SetAlgoInsideSkip{smallskip}
\title{CL-TAV algorithm specification package}
\author{CL-TAV specification (CR-2026-014 / DD-036)}
\date{}
\begin{document}
\maketitle
\noindent This package typesets the CL-TAV algorithm specification. The main
text gives the top-level session and the three core mechanisms (test selection,
timed observation interpretation, history-compatible update). The appendix gives
the S2/S6/S7--S8 coordination wrappers, the typed records, the responsibility map
and the unresolved-kernel register. It is not a second program and selects no
solver technology. Step labels \texttt{S0}--\texttt{S10} are the stable bilingual
mapping; typeset line numbers are not cited.

\input{ALG-CLTAV-01.tex}
\input{ALG-CLTAV-05-selection.tex}
\input{ALG-CLTAV-06-timing.tex}
\input{ALG-CLTAV-07-history.tex}
\input{ALG-CLTAV-02.tex}
\input{ALG-CLTAV-03.tex}
\input{ALG-CLTAV-04.tex}
\input{CLTAV_ALGORITHM_APPENDIX.tex}

\section*{Chinese step groups (same process, not a second program)}
S0 初始化：$\Gamma$ 为唯一状态源；$q$ 是 $\Gamma.\mathrm{currentSummary}$ 的别名，$qStatus$ 只记录该摘要是否已知；retry／$R_{\max}$ 分别是 $\mathrm{retryCount}$／$\mathrm{retryCap}$ 的别名；初始化 $\eta$、$H_0$、资源模式与不确定性引用。
S1／S10 停止门：EntryStop 与 PostUpdateStop 映射既有 A2--A5／P1--P5 决定表，不新建停止政策。
S2：ALG-CLTAV-02 先按返回标签分支再投影；PredictionGapError 只关掉 TEST 评分，不阻止合格 Prep／Recover。
S3：ALG-CLTAV-05 给出选择与准入规则；空 TEST 集不得 $\arg\min$；空类不阻止合格 Recover；选定后冻结不可变快照 $\xi$。
S4 一次计费：重试上限不是预算耗尽。S5：只执行快照动作一次。
S6：ALG-CLTAV-03 协调，ALG-CLTAV-06 完成时序解释；$M$、判定与候选集合 $I_z$ 是三种不同类型，不得 $H\cap$ 判定。
S7--S8：ALG-CLTAV-04 返回完整 Resolution；ERROR／未知走 RETRY，只有确认 Recover 走 COMMIT；S8 唯一写 KNOWN。
S9：ALG-CLTAV-07 在既有守卫下做整段历史相容更新并提交已确认摘要；不得旁路 S9 开始下一轮预测。
Stop-645 表示仍未在能力/实现层关闭的具名 645 依赖义务，不是“645 文件缺失”。
\end{document}
