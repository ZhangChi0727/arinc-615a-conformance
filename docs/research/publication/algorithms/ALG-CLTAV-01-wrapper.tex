\documentclass[11pt]{article}
\usepackage{fontspec}
\usepackage{xeCJK}
\setmainfont{Latin Modern Roman}
\setCJKmainfont{SimSun}
\usepackage{amsmath,amssymb}
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\usepackage[margin=1in]{geometry}
\SetAlgoNlRelativeSize{0}
\title{ALG-CLTAV-01 wrapper}
\author{CL-TAV specification (CR-2026-013 / DD-033)}
\date{}
\begin{document}
\maketitle
\noindent This wrapper exists only to compile
\texttt{ALG-CLTAV-01.tex}. It is not a second algorithm.
The procedure is the typeset form of the method-report contract.
It is not an executable engine and does not select solver technology.

\input{ALG-CLTAV-01.tex}

\section*{Chinese line groups (same process, not a second program)}
\begin{itemize}
  \item Lines 1--2: 初始化受控会话、资源模式与 $H_0$。
  \item Lines 3--8: 资源/停止检查；由准入得到 $A$ 与可选 $S$；$A$ 非空不是必可执行。
  \item Lines 9--14: $S$ 空时按 A2--A5；不得对空 minimax 集 $\arg\min$；Prep/Recover 按已接受资格。
  \item Lines 15--18: 一步 minimax 选择，一次计费，禁止未准入发出。
  \item Lines 19--26: 执行记录；ERROR 不排除 $H$、不二次计费；有效观测才做历史相容更新。
  \item Lines 27--29: 更新可观察摘要 $q$，按 Empty/Singleton/645/Equivalent/Budget 互斥停止。
\end{itemize}
Stop-645 在本增量之后表示仍未在能力/实现层关闭的具名 645 依赖义务，不是“645 文件缺失”。
\end{document}
