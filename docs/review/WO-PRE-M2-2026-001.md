# WO-PRE-M2-2026-001 — Pre-M2 baseline work list

**Purpose.** Enumerate the exact sequence of steps that must land in the specified order before M2 can be entered. Each step is single-purpose and independently verifiable. Nothing in this list authorises M2 entry — M2 begins only when the last step below is completed and the `currentStop` is moved from `PROFILE-MODEL-REFINEMENT-GATE` under a fresh CR.

- Head at time of writing: `557946ba87c3f22aaa1b8f24a8ccfded069abb87` (PR #13, Draft)
- Next stage gate to satisfy: `PROFILE-MODEL-REFINEMENT-GATE`
- Source-fidelity findings that shape the sequence: `docs/review/RR-M1-2026-004-source-fidelity.md`

Legend: `[BLOCKING]` must complete before the next step. `[PARALLEL-OK]` may run alongside a blocking step. `[OPTIONAL]` recommended but not required for the M2 entry decision.

---

## Track A — Close the current-Head external review (must run first)

### A-1 [BLOCKING] External RG0/RG1 approval on Head `557946ba…`

- Independent human reviewer (not the assistant) reads the pre-flight audit at `docs/review/RR-M1-2026-003-outcome.md`, verifies the RG0/RG1 gate checklists (WO-M1-2026-003 §5), and posts a comment on PR #13 using the WO §7 sign-off template exactly.
- Approval must bind the unchanged 40-character Head `557946ba87c3f22aaa1b8f24a8ccfded069abb87`.
- Nice-item Actions listed as `A-1..A-4` in `RR-M1-2026-003-outcome §4` may be recorded as post-approval commitments (they are M2-scope) or superseded by Track B below.
- If the reviewer records `REWORK`, halt the whole list; the corrections roll into a new work order.

### A-2 [BLOCKING] Ordinary two-parent merge into `main`

- After A-1, perform an ordinary Git merge such that the merge commit has exactly two parents and the **second parent equals `557946ba…`**. No fast-forward, no rebase, no squash.
- Trigger main-branch CI. All Python 3.10 / 3.11 / 3.12 jobs must pass on the merge commit.
- Update `activation.mergeEvidence` from `NOT-YET-PRESENT` to `ORDINARY-TWO-PARENT-MERGE` **only** on a follow-up main-branch commit that records the merge SHA and the second-parent SHA; do not backdate.

### A-3 [BLOCKING] Close CR-2026-007

- Fill the last row of the per-Head outcomes table with reviewer name, Head, RG0/RG1 outcomes, merge SHA, and timestamp.
- Set `reviewControl.rg0` and `reviewControl.rg1` to the outcome value (`APPROVED` or `APPROVED-WITH-ACTIONS`).
- Do **not** create `v4.3.2` or any other tag. Baselines are not part of M1 exit.

### A-4 [BLOCKING] `project-status.json` transition

- Move `currentStop.id` from `PROFILE-MODEL-REFINEMENT-GATE` (which was M1's next-stage gate) to the next gate in `docs/control/PROJECT_STOPS.md`. `check_repo_baseline.py` will fail if this is done before A-3.
- Update `stateChanges` wording to record M1 external verification and M2 authorisation.

---

## Track B — Source-fidelity replenishment (before A-1, or after A-2)

Two execution options; choose exactly one when you reply.

**Option B-now** — execute Track B **before** A-1. This resets the RG0/RG1 clock on Head `557946ba…` (a new Head will supersede it). Longer to full approval, but the reviewer inspects a source-fidelity-clean Head.

**Option B-after** — execute Track B **after** A-2 (post-approval, post-merge). The current Head stands, external review proceeds against `557946ba…`, and the corrections land in a new PR feeding M2 preparation. Shorter to first approval, but Findings F-1..F-4 stay open until M2 preparation begins.

### B-1 [BLOCKING within Track B] Finding F-4 — bind ARINC 664 Part 2

- Append 664 P2 to `configs/research/controlled_sources.json` `sources[]` (id `ARINC-664-2`, role `BOUNDED-PHYSICAL-LINK-REFERENCE`, publicationDate `2003-06-10`, pageCount `53`, byteCount `2983064`, `sha256=177594eed4cb8a13f370a1a3ded88baf881816f83ab0528eb435acad621063d5`, `sourceHandling=LOCAL-PROPRIETARY-NO-REPOSITORY-COPY`).
- Remove `ARINC-664-2` from `openDependencies[]`.
- Set `capabilities[ETHERNET-PHYSICAL-LINK-SEMANTICS].status` to `BOUNDED-BY-ARINC-664-2`.
- In `configs/requirements/arinc_615a3_m1_crs.json`:
  - Append `{sourceId: ARINC-664-2, role: BOUNDED-PHYSICAL-LINK-REFERENCE, sha256: 177594ee…}` to `sourceBindings`.
  - Move `DEP-ARINC-664-2` in `dependencyOpenness` to a new `boundReferences` list, or delete the openness entry.
  - No new CRS requirement is added.
- Refresh all anchor fingerprints; `sync_m1_crs.py --check` must pass.
- Commit as `data: bind ARINC 664 Part 2 as controlled physical-link reference`.

### B-2 [BLOCKING within Track B] Finding F-1 — reclassify Attachment 3 (FIND)

- For every Attachment 3 leaf unit that carries a normative FIND obligation (not pure narrative), change `applicabilityDecision` from `OUT-OF-PROFILE` to `DEFERRED-FUTURE-SCOPE` and `rationaleCode` from `NON-PROTOCOL-PRODUCT-OR-INFORMATIVE` to `DEFERRED-FIND-M9`.
- Purely narrative sentences ("This attachment describes…", figure captions, examples) stay informative.
- Add the reclassified leaves as unbound (still zero CRS requirements from Attachment 3 until FIND activation).
- Commit as `data: align Attachment 3 FIND classification with deferred profile`.

### B-3 [BLOCKING within Track B] Finding F-2 — Appendix E AFDX explicit deferral

- For every Appendix E leaf unit, change `applicabilityDecision` from `OUT-OF-PROFILE` to `APPLICABLE-CONDITIONAL` and add `deploymentCondition = "IF-AFDX-TRANSPORT-CHOSEN"`.
- Add `DEP-ARINC-664-7` to `dependencyOpenness` with `blockedCapabilities = ["AFDX-DETERMINISTIC-ETHERNET-SEMANTICS"]`.
- Cross-link the Appendix E leaves with `dependencyIds = ["DEP-ARINC-664-2", "DEP-ARINC-664-7"]`.
- If AFDX deployment is decided to be out of M1 scope entirely, use `DEFERRED-FUTURE-SCOPE` + rationale `DEFERRED-AFDX-DEPLOYMENT-M2-INFRASTRUCTURE-BINDING` instead. Record the decision in CR-2026-007 under a new DD-021.
- Commit as `data: bind Appendix E AFDX to controlled dependencies`.

### B-4 [BLOCKING within Track B] Finding F-3 — 665-5 bounded exclusion rationale

- Add a `boundedSourceScopeExclusion` array to `configs/requirements/m1_source_section_spans.json` listing 665-5 §3 through end-of-normative-body with rationale `BOUNDED-6655-SCOPE-EXCLUSION-MEDIA-SET-NOT-USED-BY-ETHERNET-UPLOAD`.
- Reference the exclusion from `profileScope.bounded665EdgePolicy.reason` (or a sibling field) so the review view surfaces it.
- Commit as `data: record explicit 665-5 media-set bounded exclusion rationale`.

### B-5 [BLOCKING within Track B] Register public IETF RFCs

- Download the eight RFCs (768, 791, 1123, 1350, 1785, 2347, 2348, 2349) from `https://www.rfc-editor.org/rfc/rfcNNNN.txt`.
- Compute SHA-256 of each, add to `configs/research/controlled_sources.json` `sources[]` with role `OPEN-PUBLIC-REFERENCE-STANDARD` and `sourceHandling=PUBLIC-TEXT-NO-REPOSITORY-COPY`.
- Remove them from `openDependencies[]`; keep their `capabilities[].status` at `NOT-ESTABLISHED` until a CRS requirement actually binds them (which is an M2 activity).
- Files themselves should be stored under `local-references/rfc/` and gitignored, or under `docs/references/rfc/` if you want them in-tree (they are public; storing them is legal). Recommend gitignored to keep the repo lean.
- Commit as `data: register IETF RFCs as controlled public references`.

### B-6 [BLOCKING within Track B] Fingerprint refresh, schema+tests, docs

- `python scripts/sync_m1_crs.py --write` to re-render the review view.
- `python scripts/sync_m1_crs.py --check`, `sync_project_overview.py --check`, `check_repo_baseline.py`, `pytest`: all must pass.
- Add two new tests:
  - `test_bound_664_p2_sha256_matches_controlled_register` — mutate the SHA and assert `sourceBindings` disagreement.
  - `test_appendix_e_afdx_carries_deployment_condition_and_dependencies` — remove the condition / dependency and assert error.
- Update `docs/control/decisions/DESIGN_DECISIONS.md` with `DD-021` (AFDX deferral decision) and `DD-022` (public RFC handling policy) if you chose those.
- Update CR-2026-007 with a "Fifth-cycle source-fidelity replenishment" section.
- Commit as `docs+test: fifth-cycle source-fidelity closure`.

### B-7 [OPTIONAL within Track B] Finding F-5 — clause label harmonisation

- Mechanical relabel of Attachment clauses to a single convention (recommend `ATTACHMENT-N.SECTION-M`).
- Run `sync_m1_crs.py --write`; update tests if they assert specific labels.
- Commit as `data: harmonise attachment clause labels`.

After B-6 (and optionally B-7), if you took Option B-now, the new Head enters Track A A-1 with the sign-off template referencing the new Head. If you took Option B-after, B-1..B-7 form a separate PR that follows the merged M1 and precedes the M2 preparation branch.

---

## Track C — Standards acquisition (parallel to Tracks A and B)

Not blocking for M1 approval or the M2 entry decision, but each acquisition unlocks the corresponding capability status.

### C-1 [PARALLEL-OK] Public RFCs

Already covered by B-5. Only depends on internet access.

### C-2 [PARALLEL-OK] Acquire ARINC 664 Part 3 (Internet-Based Protocols and Services)

- Purchase through SAE ITC (`https://www.aviation-ia.com/`) or an equivalent authorised distributor.
- Once available, add to `controlled_sources.json` `sources[]` and remove from `openDependencies[]` (analogous to B-1).
- Unblocks `AIRCRAFT-INTERNET-NETWORK-SEMANTICS`.

### C-3 [PARALLEL-OK] Acquire ARINC 664 Part 7 (AFDX / Deterministic Ethernet)

- Same channel as C-2.
- Required to fully close Appendix E AFDX at the requirement level.
- Unblocks `AFDX-DETERMINISTIC-ETHERNET-SEMANTICS` (new capability created under B-3).

### C-4 [PARALLEL-OK] Acquire ARINC 645 (Common Terminology for Data Loading)

- Same channel as C-2.
- Unblocks `CRC-VALIDATION`, `CHECK-VALUE-VALIDATION`, `NAMING-ALGORITHM-VALIDATION`, `COMPLETE-INTEGRITY-VALIDATION` (four `GAP-ARINC-645`-blocked capabilities).
- Highest utility because it is on the critical path to any integrity-related verdict.

Tracking sheet suggestion for these: create `docs/control/sources/PROCUREMENT_TRACKER.md` with one row per source (id, request date, PO / receipt, expected use in M2/M3, current owner).

---

## Track D — Actual M2 entry (only after Tracks A, and B if you chose B-now)

### D-1 [BLOCKING] New CR-2026-008 authorising M2 scope

- CR states the M2 problem: attachment reconciliation between 615A-3 (Attachment 4 timing, Appendix E AFDX) and 665-5 (LSP integrity), with the specific dispositions from `bounded665EdgePolicy` reactivated one by one.
- CR lists inputs: approved M1 CRS Head, main CI evidence, source-fidelity outcomes from B, standards acquired via C.
- CR does not authorise Configuration, execution, EFSM, or tagging.

### D-2 [BLOCKING] `project-status.json` and `roadmap` update

- Set `roadmap[M2].status` from `NEXT-BLOCKED-BY-FINAL-GATE` to `IN-PROGRESS`.
- Set `currentStop.id` to `EXECUTABLE-FOUNDATION-GATE` (M2's next-stage gate).
- `check_repo_baseline.py` must pass.

### D-3 [BLOCKING] New M2 work order

- WO-M2-2026-001 (to be drafted at that time) opens M2 with the specific attachment / infrastructure tasks.

---

## Success criteria for "pre-M2 baseline reached"

You are pre-M2-ready **iff** all of the following hold simultaneously on `main`:

1. PR #13 has been reviewed with `RG0 APPROVE|APPROVE WITH ACTIONS` and `RG1 APPROVE|APPROVE WITH ACTIONS` recorded by a named independent reviewer on an unchanged Head, using the WO §7 template.
2. That Head has been merged into `main` via an ordinary two-parent merge whose second parent equals the approved Head.
3. Main-branch CI is green on the merge commit for Python 3.10, 3.11, 3.12.
4. `check_repo_baseline.py`, `sync_m1_crs.py --check`, `sync_project_overview.py --check`, and `pytest` all pass on `main`.
5. `reviewControl.rg0`, `reviewControl.rg1`, and `activation.mergeEvidence` all reflect the actual approval and merge; no automation manufactured them.
6. CR-2026-007 is closed with the reviewer sign-off appended to the per-Head outcomes table.
7. `configurationStatus` remains `"NOT YET ESTABLISHED"`; `mergeEvidence` is now on the correct value; `certificationReady`, `authorityAccepted`, `protocolConformanceEstablished` all remain `false`.
8. If you executed Track B (Options B-now or B-after), findings F-1..F-4 are closed on the visible package on `main`.
9. Public RFCs are registered (from B-5), or C-1 completion is recorded in the procurement tracker.
10. Standards status in `capabilities[].status`: `ETHERNET-PHYSICAL-LINK-SEMANTICS` = `BOUNDED-BY-ARINC-664-2`; other capabilities remain `NOT-ESTABLISHED` unless C-2..C-4 have completed and their corresponding capabilities have been transitioned.

Once (1)-(10) are all true, M2 may be entered under D-1..D-3. Any single failure blocks M2 entry.

---

## Ordering summary

If you choose **B-now**:

`B-1 → B-2 → B-3 → B-4 → B-5 → B-6 → (B-7) → A-1 (new Head) → A-2 → A-3 → A-4 → (D-1 → D-2 → D-3 if you want M2)`

If you choose **B-after**:

`A-1 → A-2 → A-3 → A-4 → B-1 → B-2 → B-3 → B-4 → B-5 → B-6 → (B-7) → (D-1 → D-2 → D-3 if you want M2)`

Track C runs independently of both.

*Prepared 6 Sep 2026. Not an approval and not an M2 authorisation.*
