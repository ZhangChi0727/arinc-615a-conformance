% ALG-CLTAV-01 — CL-TAV top-level process (typeset form of DD-029 / DD-033).
% Not an executable engine. Interface IDs are the only call sites.
% Stable step labels S0--S10 are the bilingual mapping keys; do not cite volatile line numbers.
% Compile via ALG-CLTAV-01-wrapper.tex. Do not commit aux/log/out.
\newcommand{\IFpred}{\texttt{IF-PRED-OBS}}
\newcommand{\IFhist}{\texttt{IF-HIST-UPDATE}}
\newcommand{\IFobs}{\texttt{IF-OBS-INTERPRET}}
\newcommand{\IFsel}{\texttt{IF-SELECT-ADMIT}}
\newcommand{\IFexec}{\texttt{IF-EXECUTE-RECORD}}
\newcommand{\IFprep}{\texttt{IF-PREP-RECOVER}}
\newcommand{\IFeq}{\texttt{IF-EQUIV}}
\newcommand{\IFres}{\texttt{IF-RESOURCE-STOP}}

\begin{algorithm}[ht]
\caption{CL-TAV top-level session (ALG-CLTAV-01)}
\label{alg:cltav-01}
\SetKwInput{KwIn}{Require}
\SetKwInput{KwOut}{Ensure}
\KwIn{declared $H_0=\{h_{\mathrm{normal}}\}\cup H_{\mathrm{single}}$; SessionContext $\Gamma$; HistoryHandle $\eta$; resource mode BUDGET$(c_{\min},B)$ xor ROUNDS$(K_{\max})$; retry cap $R_{\max}$; action library; measurement uncertainty}
\KwOut{stop class, final $H$, $\eta$, session trace; singleton $h_{\mathrm{normal}}$ is not protocol PASS}
\tcp{\textbf{S0} init $H\leftarrow H_0$, $\Gamma.\mathrm{retry}\leftarrow 0$, $q$ UNKNOWN unless declared, $\eta$ empty handles}
\While{true}{
  \tcp{\textbf{S1} stop/error gate before prediction}
  \lIf{\IFres$(\Gamma,H,\eta)$ already decided Admit A2--A5 or P1--P5}{\Return that stop, $H$, $\eta$, trace}
  \tcp{\textbf{S2} predict before select; $\mathcal{C}$ feeds the select interface}
  $(\textit{pred},q,\mathcal{C})\leftarrow$ \IFpred$(\Gamma,\eta,H,\text{uncertainty},\text{library})$\;
  \lIf{$\textit{pred}$ is PredictionGapError}{\Return named prediction-gap; do not score empty $\mathcal{C}$ as remaining $\lvert H\rvert$}
  $A\leftarrow$ currently enabled actions\;
  \tcp{\textbf{S3} exclusive kind; TEST minimax never applied to Prep/Recover or empty $S$}
  $(\textit{kind},t^\star,S,\textit{admit})\leftarrow$ \IFsel$(A,q,\mathcal{C},\Gamma,\eta,H)$\;
  \uIf{$\textit{kind}=\mathrm{ADMIT}$}{
    \Return $\textit{admit}\in\{A2,A3,A4,A5\}$, $H$, $\eta$, trace\;
  }
  \uElseIf{$\textit{kind}=\mathrm{TEST}$}{
    \lIf{$S=\emptyset$}{\Return named spec error; do not $\arg\min$ empty $S$}
    $t^\star\leftarrow$ one-step minimax on currently valid nonempty classes $\mathcal{C}$ inside TEST-only $S$\;
  }
  \uElseIf{$\textit{kind}\in\{\mathrm{PREP},\mathrm{RECOVER}\}$}{
    keep the Prep or Recover $t^\star$ named by \IFsel; do not enter TEST minimax\;
  }
  \Else{
    \Return named spec error\;
  }
  \tcp{\textbf{S4} charge once; retry cap $\neq$ budget exhaustion}
  charge $t^\star$ once under the selected mode; refuse unadmitted issue\;
  \tcp{\textbf{S5} execute}
  $(\textit{record},\textit{execErr})\leftarrow$ \IFexec$(\Gamma,t^\star)$\;
  \tcp{\textbf{S6} interpret, or skip to unified ERROR}
  \eIf{$\textit{execErr}$ is ERROR}{
    $\textit{fail}\leftarrow$ true\;
  }{
    $(I_z,\textit{obsErr})\leftarrow$ \IFobs$(\Gamma,\textit{record})$\;
    $\textit{fail}\leftarrow(\textit{obsErr}$ is ERROR$)$\;
  }
  \uIf{$\textit{fail}$}{
    \tcp{\textbf{S7} unified ERROR: keep $H$; unknown effect is not a known $q$}
    do not exclude $H$; keep $\eta$; if effect unknown then $q\leftarrow$ UNKNOWN\;
    $\Gamma.\mathrm{retry}\leftarrow\Gamma.\mathrm{retry}+1$\;
    \lIf{$\Gamma.\mathrm{retry}\ge R_{\max}$}{\Return Stop-Error; this is not A5/Stop-Budget}
    continue after the one charge\;
  }
  \uElseIf{$t^\star$ is Recover}{
    \tcp{\textbf{S8} confirmation required; sent $\neq$ confirmed}
    $(\textit{confirmed},\textit{prepErr})\leftarrow$ \IFprep$(\Gamma,\eta,t^\star,\textit{record})$\;
    \eIf{$\textit{confirmed}$ is false}{
      $q\leftarrow$ UNKNOWN; identity on $\eta$ and $H$; continue\;
    }{
      proceed to S9\;
    }
  }
  \If{$I_z$ is a valid observation class}{
    \tcp{\textbf{S9} history update; $\eta'$ is the next \IFpred input}
    $(\eta,H)\leftarrow$ \IFhist$(\eta,H,t^\star,q,\mathcal{C},I_z)$\;
  }
  \tcp{\textbf{S10} exclusive P-stops; next prediction is S2, not a post-exec $q$ fake}
  let $\sigma\leftarrow$ first of Stop-Empty, Stop-Singleton, Stop-645, Stop-Equivalent, Stop-Budget via \IFres$(\Gamma,H,\eta)$ and \IFeq$(\Gamma,H,\eta)$\;
  \lIf{$\sigma$ is set}{\Return $\sigma$, $H$, $\eta$, and the trace}
}
\end{algorithm}
