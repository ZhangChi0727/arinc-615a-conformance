# CR-2026-009 — Request incremental RG1 correction of frozen M1 identities

| Field | Value |
|---|---|
| Change class | Incremental input-correction authorization request |
| Change objective | Obtain user authorization for a successor M1 delta that corrects frozen identities without rewriting the historical M1 tree |
| Source register | `configs/research/controlled_sources.json` |
| Decisions | DD-027 (primary). DD-025 remains related as the input-acceptance versus action-closure rule. |
| Candidate disposition | REQUEST-AUTHORIZATION; not an M1 rewrite and not an M2 final approval |
| Formal activation | Blocked until the user authorizes a successor input and independent incremental RG1 accepts it |
| Related | CR-2026-008, action M1-FILE-IDENTITY-6-4-4, blocking input SEQ-LUR-WRQ-ACTOR |

## Why this is required before M2 final approval

Independent visual check of 615A-3 PDF p.85 records LUR field prose at CRS-M1-00143
(clause 6.4.4). Frozen M1 Table 6.4.4-1 (CRS-M1-00309–00315) records those fields as
LUI. Shared nouns HEADER-FILE and LOAD-PART-NUMBER are not enough to emit active
665 edges. M2 therefore stopped those edges (OPEN-M1-CORRECTION). That stop is
correct and is not closure.

UPLOAD field constraints FC-LUI-FIELD-* for CRS-M1-00309–00315 currently copy the
frozen LUI identity. They must not be treated as verified LUR predicates, and they
must not be used as the sole basis for an RG1/RG2 APPROVE of this M2 candidate.
M2 does not silently overlay a second CRS authority.

This CR is one authorization request with two pending items. They may be
authorized together or separately. Neither item is executed until authorized.
Remaining M2 work is not “only waiting for this authorization”: modeled-path
Musts stay in the M2 candidate, and this request does not close them.

## Pending item A — 6.4.4 LUI/LUR file identity

Correct Table 6.4.4-1 protocol-file identity LUI→LUR if that is the accepted
visual reading, and the CRS-M1-00143 locator notes.

Affected identifiers:

- Requirements: CRS-M1-00143, CRS-M1-00309, CRS-M1-00310, CRS-M1-00311,
  CRS-M1-00312, CRS-M1-00313, CRS-M1-00314, CRS-M1-00315.
- Field constraints: FC-LUI-FIELD-FILE-LENGTH through
  FC-LUI-FIELD-LOAD-PART-NUMBER-NAME.
- Refinements: REF-A1-00143-BLOCKED-BY-FILE-IDENTITY,
  REF-A1-00315-BLOCKED-BY-FILE-IDENTITY.
- Action: M1-FILE-IDENTITY-6-4-4 stays OPEN-M1-CORRECTION until the successor
  input is authorized and independently accepted.

## Pending item B — CRS-M1-00365 WRQ actor

Independent visual check of §6.3.2 chart A reads Data Loader WRQ of LUR,
Target Hardware ACK, Data Loader DATA. Frozen CRS-M1-00365 records Target Hardware
as the WRQ actor. The M2 machine follows the TFTP-write / visual direction. That
actor mismatch is recorded as OPEN-M1-CORRECTION on the sequence edge; it is not
a rewrite of M1 bytes.

Affected identifiers:

- Requirement: CRS-M1-00365.
- Refinement: REF-SEQ-00365-WRQ-ACTOR.
- Blocking input: SEQ-LUR-WRQ-ACTOR.

## Proposed successor input (not executed)

1. Keep historical M1 merge `9bf18124d405b656815bc9eb524ae29bb4f04f56` and blob
   identities unchanged as a preserved record. Do not rewrite frozen bytes and do
   not overwrite or reuse the original merge approval on new blobs.
2. After authorization, produce a successor M1 package (new ordinary commits, new
   blob OIDs) that changes only the authorized item or items above.
3. Rebind M2 `inputAcceptance` to the successor Head/tree/blobs as a **new**
   identity. Successor `inputAcceptance` does not transplant the frozen merge
   approval. Re-open only the previously blocked edges if both ends then agree.
4. NET-ISSUE-EDITION remains a separate independent RG0 decision on P3-1 / P7
   historical editions. Recording OPEN text does not accept those editions.

## Non-claims

This CR does not authorize editing `configs/requirements/`, does not close
NET-ISSUE-EDITION, and does not request RG0/RG1/RG2 APPROVE of the current M2
Head. FIND, AFDX, ARINC 645 capability establishment, A-4, F-5 and timed
reachability remain out of this request.

# 中文版

# CR-2026-009——请求对冻结 M1 身份做增量 RG1 纠正

| 字段 | 值 |
|---|---|
| 变更分类 | 增量输入纠正授权请求 |
| 变更目标 | 在不改写历史 M1 树的前提下，取得后继 M1 增量以纠正冻结身份 |
| 来源登记 | `configs/research/controlled_sources.json` |
| 决策 | DD-027（主决策）。DD-025 仍作为输入接受与行动关闭规则相关。 |
| 候选处置 | REQUEST-AUTHORIZATION；不是改写 M1，也不是 M2 最终批准 |
| 正式激活 | 在用户授权后继输入且独立增量 RG1 接受之前保持阻塞 |
| 关联 | CR-2026-008，行动 M1-FILE-IDENTITY-6-4-4，阻塞输入 SEQ-LUR-WRQ-ACTOR |

## 为何必须在 M2 最终批准前处理

对 615A-3 PDF 第 85 页的独立目视核对显示 CRS-M1-00143（条款 6.4.4）为 LUR 字段
说明。冻结 M1 表 6.4.4-1（CRS-M1-00309～00315）将这些字段记为 LUI。共享名词
HEADER-FILE 与 LOAD-PART-NUMBER 不足以发出活动 665 边。M2 因此停线
（OPEN-M1-CORRECTION）。该停线是正确行为，不是关闭。

UPLOAD 字段约束 FC-LUI-FIELD-*（CRS-M1-00309～00315）目前复制冻结的 LUI 身份。
不得把它们当作已核实的 LUR 谓词，也不得仅凭它们对当前 M2 候选给出 RG1/RG2
APPROVE。M2 不静默覆盖第二套 CRS 权威。

本 CR 是一份授权请求，含两项待授权事项。两项可一并授权，也可分开授权。
任一事项在获授权前均不执行。剩余 M2 工作不能写成“只等本授权”：已建模路径上的
Must 仍在 M2 候选中，本请求不关闭它们。

## 待授权事项 A——6.4.4 的 LUI/LUR 文件身份

若接受目视解读，则纠正表 6.4.4-1 协议文件身份 LUI→LUR，并注明 CRS-M1-00143
定位。

受影响标识：

- 需求：CRS-M1-00143、CRS-M1-00309、CRS-M1-00310、CRS-M1-00311、CRS-M1-00312、
  CRS-M1-00313、CRS-M1-00314、CRS-M1-00315。
- 字段约束：FC-LUI-FIELD-FILE-LENGTH 至 FC-LUI-FIELD-LOAD-PART-NUMBER-NAME。
- 精化：REF-A1-00143-BLOCKED-BY-FILE-IDENTITY、REF-A1-00315-BLOCKED-BY-FILE-IDENTITY。
- 行动：在后继输入获授权并被独立接受前，M1-FILE-IDENTITY-6-4-4 保持
  OPEN-M1-CORRECTION。

## 待授权事项 B——CRS-M1-00365 的 WRQ 参与者

对 §6.3.2 图 A 的独立目视核对显示数据加载器发出 LUR 的 WRQ、目标硬件 ACK、
数据加载器 DATA。冻结 CRS-M1-00365 将 WRQ 参与者记为目标硬件。M2 机器遵循
TFTP 写／目视方向。该参与者不一致作为序列边的 OPEN-M1-CORRECTION 记录，不是
改写 M1 字节。

受影响标识：

- 需求：CRS-M1-00365。
- 精化：REF-SEQ-00365-WRQ-ACTOR。
- 阻塞输入：SEQ-LUR-WRQ-ACTOR。

## 拟议后继输入（尚未执行）

1. 将历史 M1 合并 `9bf18124d405b656815bc9eb524ae29bb4f04f56` 及 blob 身份作为
   保留记录保持不变。不改写冻结字节，也不把原合并批准覆盖或复用到新 blob。
2. 授权后只生成后继 M1 数据包（新的普通提交与新 blob），仅变更已授权的上述
   事项。
3. 将 M2 `inputAcceptance` 作为**新身份**重新绑定到后继 Head／树／blob。后继
   `inputAcceptance` 不移植冻结合并批准。仅当两端一致时才重开先前阻塞的边。
4. NET-ISSUE-EDITION 仍是关于 P3-1／P7 历史版次的独立 RG0 决定。记录 OPEN 文字
   不等于接受这些版次。

## 非主张

本 CR 不授权编辑 `configs/requirements/`，不关闭 NET-ISSUE-EDITION，也不请求对
当前 M2 Head 做 RG0/RG1/RG2 APPROVE。FIND、AFDX、ARINC 645 能力建立、A-4、F-5
和定时可达不属于本请求。
